<div class="flex flex-col items-center mt-[30px]">
  <% if @user.image.attached? %>
    <%= image_tag @user.image.variant(resize: "80x80^", gravity: "center", crop: "100x100+0+0"), class: "rounded-full object-cover" %>
  <% else %>
    <%= image_tag 'ディフォルト画像2.png', size: '150x150', class: "rounded-full object-cover" %>
  <% end %>
  <p class="mt-2 text-center text-xl font-bold"><%= @user.name %></p>
</div>

  
    <div class="flex justify-center items-center">
    <div class="w-full max-w-4xl bg-white shadow-md p-6 rounded-md rounded-xl my-8">     

    <div class="flex justify-center border-b border-gray-200">
    <%= link_to "全て", "#", class: "w-1/2 text-center text-teal-500 text-xl pb-1 border-b-2 border-teal-500 hover:text-teal-700" %>
    <%= link_to "お気に入り", "#", class: "w-1/2 text-center text-gray-400 text-xl pb-1 hover:text-teal-500 hover:border-b-2 hover:border-teal-500" %>
  </div>
  

  
    </div>
</div>
 
<ol class="restaurants">
<%= render @restaurants %>
</ol>
<%= will_paginate @restaurants %>