<%= @user.name %>　様

この度はRestaurantChooserにご登録いただきありがとうございます。
アカウント作成完了の為に、以下のリンクをクリックしてください。

<%= edit_account_activation_url(@user.activation_token, email: @user.email) %>

また、本メールに心当たりのない方はお読み捨てください。 